# using tensorflow for CNN, since scikit doesn't have CNN support because
# of out of subject and stuff blah blah

import os
import tensorflow as tf
from tensorflow.keras import datasets, layers, models
import matplotlib.pyplot as plt
